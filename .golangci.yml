version: "2"

linters:
  enable:
    - gocyclo
    - gocognit
    - govet
    - errcheck

formatters:
  enable:
    - gofmt

linters-settings:
  gocyclo:
    min-complexity: 10
  gocognit:
    min-complexity: 15

run:
  timeout: 1m
